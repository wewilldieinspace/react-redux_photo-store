export interface IItem {
    name: string,
    model: string,
    category: number,
    shortDescription: string,
    description: string,
    features: string[],
    price: number,
    images: string[],
    id: number
}